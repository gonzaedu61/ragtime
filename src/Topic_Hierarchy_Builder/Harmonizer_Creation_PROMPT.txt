The uploaded code clusters text chunks extracted from a Vextor DB and generates a semantic hierarchy. The output hierarchy is saved on a json file. I need you to generate a new Python class script to harmonize the lower levels by doing the following adjustemnt to the hierarchy according to the given input, definitions and task rules:

INPUT DATA:
1 - The output hierarchy json file generated by the here uploaded code
2 - A "max_tokens" value
3 - A "min_tokens" value

DEFINITIONS:
1 - A cluster is a "Leaf" cluster if it has no children clusters.
2 - A cluster is an "Internal" cluster if it has at least one children cluster
3 - A cluster is a "Leafs_Parent" cluster if it is "Internal" and all of its children are "Leaf" clusters
4 - A cluster is a "Group" cluster if it is "Internal", and its children are a mix of one or more "Leafs_Parent" and one or more "Leaf" clusters, and it is no more than two levels upwards from any of all of its contained "Leaf" clusters downwards 
5 - An "Orphan_Leaf" cluster is a "Leaf" cluster direct children (one level below) of a "Group_Parent" cluster

TASK RULES:
1 - Find all "Group" clusters with a total count of tokens (adding up all leaf cluster tokens downwards) greater than "max_tokens". These will be identified each as a "Big_Group" cluster
1.a - "Leaf" cluster tokens are calculated adding up the tokens of the associated chunk tokens.
1.b - The chunk tokens can be obtained from the Vector DB using the chunk_id. 
1.cc - The chunk_ids of a "Leaf" cluster are indicated as an attribute of the Leaf cluster in the input json hierarchy file
2 - Split each "Big_Group" cluster in smaller clusters by grouping its direct children considering:
2.a Semantic closeness
2.b The total count of tokens should be as much as possible within the [min_tokens,max_tokens] range
2.c As a new level will be introduced by this child regrouping, re-number all the cluster ids downwards as needed.

OUTPUT:
1 - Generate the new adjusted hierarchy json file with its name being <input_json_name>_harmonized.json
2 - Generate a log file indicating what original "Big_Group" clusters were split and the details of the child grouping performed.

