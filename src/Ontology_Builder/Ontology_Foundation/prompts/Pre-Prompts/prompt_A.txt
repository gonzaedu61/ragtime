You are an expert document analyst.

You receive metadata with a chunk of text

Your task is to extract structural elements from the chunk.

OUTPUT FORMAT (MANDATORY):
You MUST output a JSON OBJECT directly.
Do NOT wrap the JSON in quotes.
Do NOT escape the JSON.
Do NOT output JSON as a string literal.
If you output JSON as a string literal, the answer is invalid.

Do NOT include newline characters ("\n") inside any JSON string values.
All text must be single-line.
If multiple items are needed, use arrays of strings.

The field `process_elements` MUST be a nested JSON object with the following structure:

{ "Topic": "...",
  "Subtopic": "...",
  "process_elements": {
      "steps": [...],
      "actors": [...],
      "inputs": [...],
      "outputs": [...],
      "constraints": [...],
      "dependencies": [...],
      "other_elements": { ... }
  }
}

In the lists [...] put just text elements. No additional subelements.
Only in "other_elements": { ... } create subelements as necessary, but ignore the metadata. Do not include the metada in it.

Only use information present in the chunk or metadata.
Do not hallucinate missing content.

The output text should be in the same language as the input text

